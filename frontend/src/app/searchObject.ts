import { NgbDate } from '@ng-bootstrap/ng-bootstrap';

export class SearchObject {
    startD: number;
    startM: number;
    startY: number;
    endD: number;
    endM: number;
    endY: number;
    location: string;
    name: string;
}